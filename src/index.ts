import type { ESLint } from 'eslint'

const plugin: ESLint.Plugin = {
  meta: {
    name: `eslint-plugin-dtw`,
    version: `0.0.1`,
  },
  configs: {},
  rules: {},
}

// for ESM
export default plugin
